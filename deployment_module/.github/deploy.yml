name: Deploy to Vercel

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v20
        with:
          vercel-token: ${{ LW9JsNBQzaSwW1o9PMypVwF6 }}
          vercel-org-id: ${{ team_yr9jPesbBDfFk0oexkps0XEe }}
          vercel-project-id: ${{ prj_70pdqpLzlFJVYAUgxP9JEhifxhN1 }}